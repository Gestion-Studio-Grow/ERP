43,46c43,45
<  * El importe se compara en centavos, redondeado como viaja a ARCA (la regla única
<  * de src/lib/dinero/redondeo.ts, la misma con la que src/plugins/arca/afip/soap.ts
<  * escribe WSFEv1): el umbral se decide sobre el mismo número que recibe ARCA, no
<  * sobre decimales que nunca llegan.
---
>  * El importe se compara en centavos, redondeado como viaja a ARCA (`toFixed(2)`,
>  * el formato de WSFEv1 en src/plugins/arca/afip/soap.ts): el umbral se decide
>  * sobre el mismo número que recibe ARCA, no sobre decimales que nunca llegan.
71d69
< import { centavosDe } from "@/lib/dinero/redondeo";
455,456c453,455
<  * con 2 decimales y el plugin los escribe con la misma regla (`textoAlCentavo`).
<  * Así 5.549.861,995 cuenta como 5.549.862,00, que es lo que ARCA recibe.
---
>  * con 2 decimales y el plugin los arma con `toFixed(2)`
>  * (src/plugins/arca/afip/soap.ts, `fmt`). Así 5.549.861,995 cuenta como
>  * 5.549.862,00, que es lo que ARCA recibe.
458c457
< const aCentavos = centavosDe;
---
> const aCentavos = (n: number) => Math.round(Number(n.toFixed(2)) * 100);
29d28
< import { redondearAlCentavo } from "@/lib/dinero/redondeo";
651c650
< test("el importe se decide con 2 decimales, como viaja a ARCA (la regla única de redondeo del plugin)", () => {
---
> test("el importe se decide con 2 decimales, como viaja a ARCA (toFixed(2) del plugin)", () => {
660c659
<     const viaja = redondearAlCentavo(importeTotal);
---
>     const viaja = Number(importeTotal.toFixed(2));
664,671c663,664
<   // Medio centavo sube (ENG-109): 9.999.999,995 viaja como 10.000.000,00 y hay que identificar.
<   assert.ok(codigos(decidirComprobante(RI, CF, productos({ importeTotal: 9_999_999.995 }))).includes("IDENTIFICACION_OBLIGATORIA"));
<   // Igual con la regla del negocio. 599.999,995 es, en binario, 599.999,99499…, pero vale
<   // 599.999,995 por sus 15 cifras y viaja como 600.000,00: alcanza el umbral de 600.000.
<   assert.ok(
<     codigos(decidirComprobante(MONO, CF, productos({ importeTotal: 599_999.995 }), { umbralIdentificacionDelNegocio: 600_000 }))
<       .includes("IDENTIFICACION_REGLA_DEL_NEGOCIO"),
<   );
---
>   // Igual con la regla del negocio. 599.999,995 es, en binario, 599.999,99499…: viaja como
>   // 599.999,99 (debajo de 600.000), aunque Math.round(importe * 100) diría 600.000,00.
673c666
<     const viaja = redondearAlCentavo(importeTotal);
---
>     const viaja = Number(importeTotal.toFixed(2));
