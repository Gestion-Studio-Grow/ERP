# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# …salvo la plantilla SIN secretos (solo nombres + comentarios) para Vercel:
!.env.vercel.template

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# local sqlite leftovers (proyecto ya usa Postgres)
*.db
*.db-journal

# Local Netlify folder
.netlify

# Material confidencial — NUNCA versionar (repo público)
_private/
docs/internal/

# PDFs generados por los generadores del panel de la célula (artefactos, no versionar)
celula-negocios-digitales/panel/*.pdf
