# Respaldo antes del pase a producción — 2026-09-23

Lo que había en producción antes del primer pase del ERP por apps (olas 1 a 3 y los arreglos
de seguridad). Si algo sale mal, desde acá se vuelve.

## Código

| Qué | Dónde |
|---|---|
| Commit en producción | `0476abe9ee4a77bfdc3a4909b9cf85b49421ced0` (main) |
| Respaldo inmutable en GitHub | rama `respaldo/produccion-2026-09-23` en Gestion-Studio-Grow/ERP |
| Deploy de Vercel vigente | `dpl_Cz3NnYeQiKCygoN9Ny4ayJh315gv` (proyecto `erp-ch`) |

La etiqueta `respaldo-produccion-2026-09-23` no se pudo subir (el proxy de git del entorno
rechaza tags con 403); la rama cumple la misma función.

## Base de datos (lo hace el dueño, un clic)

Neon → Branches → *Create branch* desde producción → nombre `respaldo-2026-09-23`.
Es una copia completa e instantánea. Además Neon guarda la historia de la base y permite
restaurar a un momento anterior dentro de su ventana de retención.

## Cómo volver atrás

1. **Código:** Vercel → erp-ch → Deployments → `dpl_Cz3NnYeQiKCygoN9Ny4ayJh315gv` → *Instant
   Rollback* (en Hobby sólo llega al deploy inmediatamente anterior: hacerlo PRIMERO).
   Alternativa: llevar `main` al commit de la rama `respaldo/produccion-2026-09-23`.
2. **Base:** el lote sólo agrega columnas, tablas e índices; el código viejo funciona con la base
   migrada, así que normalmente NO hace falta tocar la base. Si hiciera falta: Neon → restaurar
   desde el branch `respaldo-2026-09-23`. **Restaurar borra lo cargado después del respaldo**
   (cobros de CH incluidos): se hace sólo si no hay otra salida, y se anota la hora.

## Variables de Vercel al momento del respaldo (sólo nombres, sin valores)

Production: NAV_GROUPING_ENABLED, FISCAL_MASTER_KEY, TENANT_HOST_MAP, MP_WEBHOOK_SECRET,
MP_ACCES_TOKEN, ARCA_INVOICING_ENABLED, ARCA_MODO, ARCA_KEY_PEM, ARCA_CERT_PEM,
OPERATOR_PASSWORD, OPERATOR_SECRET, OPERATOR_DATABASE_URL, DATABASE_URL, RLS_ENFORCEMENT,
ADMIN_PASSWORD, AUTH_SECRET. (Preview compartía casi todas, incluida la base.)
