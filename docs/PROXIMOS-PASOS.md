# Próximos pasos — cola de handoff entre sesiones

**Qué es:** la cola donde cada sesión deja el trabajo concreto que disparó, para que la
sesión siguiente lo lea del repo en vez de re-descubrirlo en una charla. Es la pieza que
faltaba de ADR-008: las decisiones ya persistían como ADR y las features en BACKLOG, pero
el *handoff* ("qué sigue después de cerrar esto") vivía en el chat — lo único que la sesión
siguiente no lee. Ver **ADR-016**.

**Cómo se usa (lo hacen los comandos solos, no a mano):**
- **Al abrir** una sesión: el comando lee esta cola y ofrece los ítems abiertos que le tocan
  a su tipo como punto de arranque por defecto, antes de preguntar en blanco.
- **Al cerrar** una sesión: si disparó un follow-up concreto, lo agrega acá con el comando
  sugerido; si completó un ítem de la cola, lo marca hecho.
- **Al consolidar:** `/sesion-consolidacion` poda esta cola (saca lo hecho, revalida lo abierto).

**Convención de cada ítem:** `[ ]` abierto / `[x]` hecho · comando sugerido · descripción ·
`(origen: <tipo de sesión> — <fecha>)`. Lo hecho se deja tildado hasta que consolidación lo
barre, así queda rastro de una sesión a la otra.

---

## Abiertos

- [ ] `/sesion-feature RBAC Fase 2 (ADR-017)` — autorización fina sobre la identidad ya construida en Fase 1: `requireUser()`/`requireRole(...)` al tope de cada Server Action y loader de `/admin` (mapa rol→permisos en código), ocultar en el front lo que RECEPTION/PROFESSIONAL no ven (UX, no seguridad), pantalla de gestión de usuarios para el OWNER (alta/baja, rol, reset password), y retirar `ADMIN_PASSWORD` (ya no es puerta sin dueño). *(origen: sesión de feature — 2026-07-03)*
- [ ] Cambiar el email **provisional** del OWNER (`macarenaarias21@gmail.com`) por el real de Carolina cuando lo tengamos — hoy es solo el identificador de login; se edita desde la gestión de usuarios (Fase 2) o con un update directo. *(origen: sesión de feature — 2026-07-03)*
- [x] `/sesion-feature Plugin ARCA — emisión con CAE (ADR-019/020) — Fase 1 Core-side` — **hecho** este ciclo (gated, inerte en prod): modelos `FiscalDocument`/`TenantFiscalConfig`/`TenantJurisdiccion`/`OutboxEvent` (todos con `tenantId`) + **migración generada offline, NO aplicada** (`prisma/migrations/20260704000000_add_localizacion_ar_fiscal`); `FiscalConnector` por capacidades + namespace `ar.nacional` (ADR-020 D2); pipeline `requestFiscalComprobante` (crea doc+outbox en una tx, intento síncrono B3) + comandos idempotentes + `drainOutbox`; route `/api/localizacion-ar/drain` (patrón CRON_SECRET); **motor de cálculo fiscal** en el Core (ADR-006, `calculo-fiscal.ts`): multi-alícuota con desglose por alícuota (array `Iva[]` de WSFEv1), catálogo de códigos ARCA (Id 3/4/5/6/8/9), aritmética en centavos enteros sin deriva de float; juego completo de importes WSFEv1 (`neto`/`exento` ImpOpEx/`noGravado` ImpTotConc/`iva`/`total`, con `total = suma` exacto); validación **fail-closed** (lanza ante líneas vacías, importe negativo/NaN, alícuota inválida, total ≤ 0); auto-chequeo de invariantes `assertConsistente` (defensa en profundidad); desglose e importes persistidos en `FiscalDocument`. **Identidad fiscal** (`identidad-fiscal.ts`): validación de CUIT por dígito verificador (fail-closed en el pipeline: no emite con CUIT roto), formateo, y mapeo de **condición IVA del receptor** a códigos ARCA (RG 5616 — obligatorio, rechazo desde 1/9/2026) propagado en `EmisionInput.receptorCondicionIvaId`. **QA:** suite `npm test` (`node:test` vía tsx, sin deps nuevas) — **22 casos** en `calculo-fiscal.test.ts` (17: catálogo, alícuotas, multi-alícuota, redondeo, exento/no-gravado, C vs B, validación, invariantes) + `identidad-fiscal.test.ts` (5: CUIT válido/ inválido/formato, mapeo RG 5616). Enganche en `confirmPayment` detrás de `LOCALIZACION_AR_ENABLED`. Conector real = stub de homologación (CAE ficticio) hasta cablear AfipSDK. tsc+build limpios; motor verificado (18/18: multi-alícuota, redondeo, C vs B). *(origen: sesión de arquitectura — 2026-07-04 · cerrado 2026-07-04)*
- [ ] `/sesion-feature Plugin ARCA — puesta en producción (homologación + AfipSDK)` — lo que falta para emitir real: (1) adaptador **AfipSDK** que implemente `FiscalConnector.emitir` (WSAA+WSFEv1) y registrarlo en `connectors/index.ts` en lugar del stub — **mapeando `EmisionInput.ivaDetalle` al array `Iva[]` de WSFEv1** (Id/BaseImp/Importe), que el motor de cálculo ya produce; validar los Ids contra `FEParamGetTiposIva` en runtime; (2) **aplicar la migración** `20260704000000_add_localizacion_ar_fiscal` en un **branch de Neon** primero, después en prod; (3) cargar `TenantFiscalConfig` del tenant de estética (CUIT, punto de venta, `ambiente=HOMOLOGACION`) con la credencial **fuera del repo**; (4) **ensayo obligatorio contra homologación de ARCA** antes de pasar `ambiente=PRODUCCION`; (5) activar `LOCALIZACION_AR_ENABLED` + programar la Scheduled Function de Netlify que pega a `/api/localizacion-ar/drain`; (6) front: mostrar el CAE/nº de comprobante en el detalle del pago. Cierra la Fase 2 del piloto. Insumos: ADR-019/020 + código en `src/lib/localizacion-ar/`. *(origen: sesión de feature — 2026-07-04)*
- [ ] `/sesion-arquitectura jerarquía de tenants (organización de contador → N tenants de clientes) + provisioning en lote` — la decisión estructural que dispara el producto para estudios contables: un contador es una organización dueña de N tenants (sus clientes), con alta en lote. Hoy provisioning no existe (INDEX lo lista como inexistente). Insumo: `docs/facturador-estudios-contables-mercado-y-vision.md`. *(origen: sesión de negocio — 2026-07-04)*
- [ ] **Validación comercial (negocio, GATE previo a construir el producto para contadores):** entrevistar 2–3 contadores reales sobre el wedge "el cliente me manda la info ya limpia" y a qué suite impositiva (Xubio/SOS-Contador/Bejerman) querrían la exportación. No abrir desarrollo hasta cerrar esto — riesgo de foco sobre el piloto. **Kit listo:** guía de reunión (`docs/reunion-contador-guia-exploracion.md`), diseño del producto (`docs/diseno-producto-middleware-fiscal.md`) y demo clickeable (`docs/demo/panel-contador-demo.html` / artifact). **1ª reunión agendada: estudio grande que puede traer clientes (2026-07-05).** Al volver, cargar los 4 datos del cierre de la guía + confirmar si el estudio ya tiene delegación de servicios en ARCA (§15 del diseño). Insumos: `docs/facturador-estudios-contables-mercado-y-vision.md` §4/§5. *(origen: sesión de negocio — 2026-07-04)*
- [ ] `/sesion-feature activar RLS de Postgres (ADR-018)` — **disparo: cuando se provisione el 2º tenant, no antes.** Migración con `ENABLE ROW LEVEL SECURITY` + policies `USING (tenant_id = current_setting('app.current_tenant_id'))` en cada tabla de negocio; rol de app sin `BYPASSRLS`; extensión de Prisma (`$allOperations`) que envuelve cada operación en `$transaction` con `SET LOCAL app.current_tenant_id`; resolución de tenant por request (subdominio/sesión) en `tenant.ts` conservando el assert fail-closed como red. **Ensayo obligatorio en branch de Neon con tenant sintético antes de tocar producción.** *(origen: sesión de arquitectura — 2026-07-03)*

## Hechos (pendientes de poda por `/sesion-consolidacion`)

- [x] `/sesion-feature RBAC Fase 1 (ADR-017)` — hecho y deployado: `User`+`UserRole` (migración `20260703170000_add_users_rbac` aplicada a Neon), login por email+password (cookie con `userId`, scrypt), `getCurrentUser()`, `actor` real en el audit. OWNER de Carolina sembrado (email provisional). tsc+build limpios, login verificado en preview. Falta Fase 2 (ver Abiertos). *(origen: sesión de feature — 2026-07-03 · cerrado 2026-07-03)*
