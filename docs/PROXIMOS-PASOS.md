# Próximos pasos — cola de handoff entre sesiones

**Qué es:** la cola donde cada sesión deja el trabajo concreto que disparó, para que la
sesión siguiente lo lea del repo en vez de re-descubrirlo en una charla. Es la pieza que
faltaba de ADR-008: las decisiones ya persistían como ADR y las features en BACKLOG, pero
el *handoff* ("qué sigue después de cerrar esto") vivía en el chat — lo único que la sesión
siguiente no lee. Ver **ADR-016**.

**Cómo se usa (lo hacen los comandos solos, no a mano):**
- **Al abrir** una sesión: el comando lee esta cola y ofrece los ítems abiertos que le tocan
  a su tipo como punto de arranque por defecto, antes de preguntar en blanco.
- **Al cerrar** una sesión: si disparó un follow-up concreto, lo agrega acá con el comando
  sugerido; si completó un ítem de la cola, lo marca hecho.
- **Al consolidar:** `/sesion-consolidacion` poda esta cola (saca lo hecho, revalida lo abierto).

**Convención de cada ítem:** `[ ]` abierto / `[x]` hecho · comando sugerido · descripción ·
`(origen: <tipo de sesión> — <fecha>)`. Lo hecho se deja tildado hasta que consolidación lo
barre, así queda rastro de una sesión a la otra.

> **Estados y % de avance:** esta cola lista trabajo **nuestro** pendiente (🟢 Avanzable ya). Lo
> que del lado dev está terminado y solo espera una **acción humana** (credenciales, aplicar
> migración, OK de gate) **no va acá como deuda** — se reporta como estado en `docs/ESTADO-FRENTES.md`
> según `docs/METODOLOGIA-REPORTE-AVANCE.md`. El mapa de "qué más encarar" vive en ese mismo doc
> (backlog de frentes a conversar).

> **Sprint de 5 equipos:** cuando el owner tipea `sprint` desde el móvil se abren 5 frentes en
> worktrees aislados (modelo en `docs/METODOLOGIA-SPRINT.md` + `/sprint`). Cada equipo deja su
> handoff en `## Sprint activo` de `docs/SPRINT-MOVIL.md`; el PMO integra a `main`.

---

## Abiertos

- [ ] **DIRECCIÓN — deploy del Panel de Dirección (ADR-028) = Gate 1 (OK de Maxi).** La estructura ya está en el repo: ruta protegida `/operador/direccion` (+ `/operador/direccion/panel` a pantalla completa), `panel.generated.ts` bundleado y link en la nav del control-plane. Fuente de verdad `celula-negocios-digitales/panel/panel.html`; republicar con `npm run publicar:direccion` cuando cambie. **Sin migración → no hay Gate 2**; solo falta el **OK de deploy a producción de Maxi (Gate 1)**. Follow-ups diferidos (ADR-028 §4): auth propia de dirección si no alcanza el login de operador, y ofrecer los PDF (manual/plantillas/costos) como descargas desde la ruta. *(origen: sesión de arquitectura — 2026-07-07)*
- [x] **PLATAFORMA — Observabilidad v1 + Reportes v2 export — HECHO 2026-07-05 (frente Core Plataforma).** (a) **Reportes v2**: KPIs profundos para el dueño (no-show, cancelación, ticket promedio, retención, rentabilidad hora-silla, mix de método de pago) en `src/lib/report-kpis.ts` (puro, 8 tests) + `getDeepReportData` + tarjetas en `/admin/reportes`; **export CSV** (`src/lib/report-csv.ts` puro + 4 tests, route `/admin/reportes/export`, BOM+`;` para Excel es-AR, mismo `reports:read`). (b) **Observabilidad**: `src/lib/logger.ts` — logger JSON estructurado, cero deps (ADR-008), serialización pura + 6 tests; los ~6 `console.error` dispersos (audit/arca/external-orders/3 API routes) migrados con contexto (tenantId/actor/ids); health endpoint `GET /api/health` **shallow** (sin DB, respeta Neon free). (c) **Tenancy hardening offline**: `extractSubdomain` cubierto (8 tests) para de-riscar el gate del 2º tenant. Verde: tsc + 39 tests nuevos + build. *(origen: frente Core Plataforma — 2026-07-05)*
- [ ] **COORDINACIÓN PMO — secuencia de activación de RLS.** El frente Core Plataforma **NO tocó la activación de RLS** (cimientos): el SQL+wiring siguen listos y apagados (ítems de RLS abajo, ADR-018). La activación es Gate 2 y depende del alta del 2º tenant — **la secuencia la decide el PMO/owner**, no este frente. Pendiente del PMO: ordenar activación de RLS + rotación de `DATABASE_URL` a `app_user` + ensayo en branch de Neon junto con el provisioning del 2º tenant. *(origen: frente Core Plataforma — 2026-07-05)*
- [ ] **Rama `feature/ux-ui-upgrade` con 1 commit SIN mergear a main** (`242011a docs: design system del upgrade UX/UI + estado de retome`). Detectado al limpiar worktrees viejos (2026-07-05): el *código* del design system ya está en main, pero ese commit de **docs** quedó solo en la rama. Revisar su contenido y o bien mergearlo (cherry-pick a main) o descartar la rama si es redundante. No se borró la rama para no perder el commit. *(origen: setup de sprint / PMO — 2026-07-05)*

- [ ] `/sesion-feature Plugin ARCA — lado Core` — **código escrito y verificado offline (tsc + build); la migración queda SIN aplicar (Gate 2).** Ya hecho: (a) modelos `Invoice` + `OutboxEvent` (+ enum `InvoiceStatus`) en `prisma/schema.prisma` + migración `20260704160000_add_invoice_outbox` **escrita a mano, NO aplicada a Neon**; (b) `src/lib/invoice-core.ts` — `createInvoice` (crea factura + encola `InvoiceCreated` en la misma transacción, outbox ADR-002), `registerFiscalDocument` (comando que el plugin invoca con el CAE), `markInvoiceRejected`, `getInvoice`; (c) `src/lib/arca-dispatch.ts` — `processArcaOutbox()` drena el outbox y corre el handler del plugin con `clientePara` (hoy stub) + `registerFiscalDocument`. **Falta (todo pendiente de una sesión con DB/decisión de negocio):** (1) correr `prisma migrate deploy` en Neon (Gate 2, OK explícito); (2) enganchar `createInvoice` a un flujo real de negocio (¿al `confirmPayment`? decisión de producto, a propósito NO cableado); (3) worker periódico que llame `processArcaOutbox` (pg-boss/graphile-worker, ADR-002/005 — hoy es a demanda); (4) `clientePara` real: credenciales ARCA **por tenant** + adapter SOAP (ver ítem de abajo); (5) auth del path worker (no hay sesión de usuario — hoy sin `requireCapability`, decidir modelo). *(origen: sesión de arquitectura — 2026-07-04)*
- [ ] **⚠️ COMMITEAR el wiring de `completeAppointment` (ADR-024) — quedó en el working tree SIN commitear.** El disparo de facturación al cerrar el servicio (toggle `facturar` + guarda del flag + llamada a `facturarAppointment`) está **aplicado en `src/lib/actions.ts` pero no se commiteó**: el archivo estaba co-editado por la sesión POS/RLS y tenía un bloque `DEV-ONLY ... REVERTIR antes del push` en `getDashboardData` que **no debe ir a `main`**. Sin `git add -p` no pude separar mi hunk del suyo. **Acción:** cuando `actions.ts` se reconcilie (revertido el bloque dev), commitear el hunk de `completeAppointment` (imports de `@/lib/fiscal` y `@/lib/invoice-from-appointment` + toggle + bloque best-effort). El código exacto está en ADR-024 §2. Todo lo demás de ADR-024 (plugin MP, `fiscal.ts`, `invoice-from-appointment.ts`, `mercadopago-dispatch.ts`, webhook) **sí** se commiteó. *(origen: sesión de arquitectura — 2026-07-04)*
- [ ] `/sesion-feature ARCA/MP — completar puesta en marcha (ADR-024)` — con la migración de `Invoice` aplicada (Gate 2) y `ARCA_INVOICING_ENABLED` prendido: (1) **checkbox de UI** "No facturar" en el form de completar de `AppointmentRow.tsx` (`<input type="checkbox" name="facturar" value="false">`); (2) worker periódico que drene el outbox (hoy tick inline en `facturarAppointment`, simulador); (3) adapter real de MP + credenciales por tenant + **validar firma `x-signature`** del webhook; (4) perfil fiscal real (hoy `src/lib/fiscal.ts` provisional: Monotributo/CUIT placeholder) → config por tenant; (5) capturar CUIT/DNI del `Client` para Factura A/B (hoy todo Consumidor Final → Factura C). *(origen: sesión de arquitectura — 2026-07-04)*
- [ ] **PRODUCTO — `/sesion-feature Ingesta MP masiva + facturación automática (ADR-025)`** — arca como **solución completa** para **monotributistas de alto volumen operable por contadores**: detectar TODO lo que entra por MP, **clasificarlo**, facturar solo las ventas, con panel del contador y alertas fiscales. **Núcleo con stubs ya en `main`** (`src/plugins/mercadopago/`): `sincronizarPagos` (ingesta idempotente **con clasificador cableado**), `ClasificadorPorReglas` (§12.1), `ReconciliacionPort` (estados FACTURADO/NO_FACTURABLE/REVISAR/ERROR), `NotificacionWhatsAppStub` (§12.4), `simularFeedMonotributista`; `src/lib/invoice-from-mp.ts` = `facturarPagoMP`. **Avance con simulador (ADR-025 §12-bis, en main):** OAuth stub (`oauth.ts`: `vincularCuenta` + `CredencialesConRefresh`, tokens por tenant), end-to-end clasificación→facturación con CAE por stub ARCA (`src/lib/mercadopago-simulador.ts`), y **scaffold del panel del contador** en `src/app/contador/` (+ `src/lib/contador-panel.ts`, datos simulados). **Roadmap (ADR-025 §12):** — **MVP:** (1) tabla de conciliación `payment_id→invoice_id` unique por tenant + migración (Gate 2, reemplaza el stub en memoria); (2) **reglas de clasificación** persistidas + señal `operacion` real del pago MP; (3) adapter real MP con **OAuth** (authorization code, §9) + API pagos + firma webhook; (4) worker de cola con **rate-limit**/batch WSFEv1. — **v1+:** (5) **panel del contador** multi-cliente (cartera, facturado/no/revisar, aprobar en lote, §12.2); (6) **alerta de recategorización** de monotributo (acumulador facturado por cliente/período, avisar al 80% del tope, §12.3); (7) **confirmación por WhatsApp** real (proveedor + webhook de respuesta sí/no, §12.4); (8) reglas de clasificación configurables por comercio. — **visión:** aprendizaje de clasificación, agrupado, otros medios de pago. Todo gateado por el flag + credenciales reales. *(origen: sesión de arquitectura — 2026-07-04)*
- [ ] `/sesion-feature Plugin ARCA — adapter real WSAA+WSFEv1` — implementar `src/plugins/arca/afip/soap.ts` (hoy solo existe el stub): CMS firmado con el certificado X.509 para el WSAA (riesgo técnico #1), cache de Token+Sign 12h, y `FECompUltimoAutorizado` + `FECAESolicitar` de WSFEv1. **Validar contra homologación de ARCA antes que producción.** Independiente del lado Core (corre contra el stub hasta que haya certificado). *(origen: sesión de arquitectura — 2026-07-04)*
- [ ] Cambiar el email **provisional** del OWNER (`macarenaarias21@gmail.com`) por el real de Carolina cuando lo tengamos — hoy es solo el identificador de login; se edita desde la gestión de usuarios (Fase 2) o con un update directo. *(origen: sesión de feature — 2026-07-03)*
- [x] **Script de provisioning (ADR-019) — implementado, verificado y en `main`.** `scripts/provision-tenant.ts` (+ npm `provision`): idempotente por `slug`, transaccional (todo-o-nada), aditivo, parametrizado (name/slug/OWNER/timezone/**branding**), con el gate ADR-018 que **se niega a crear la 2ª fila de `Tenant` sin RLS activo**; siembra `Tenant` + `User` OWNER (scrypt, ADR-017, contraseña de bootstrap fuera del repo — se genera y se imprime una vez si no se pasa) + **`BusinessSettings` (branding/localización, hoursLabel Lun–Sáb 9–19 por defecto)** + catálogo blueprint mínimo editable (box + categoría "General" + 2 servicios de ejemplo + profesional de ejemplo con WorkingHours Lun–Sáb 9–19). No comparte código con `prisma/seed.ts`. **Refactor sobre la versión de `feature/performance`:** core `provisionTenant(prisma, params)` inyectable/testeable + `main()` CLI que solo corre si se ejecuta el archivo directo. **Verificado en LOCAL** (Postgres efímero PGlite vía socket, sin tocar Neon): 27 checks en verde — alta, idempotencia (sin duplicar, sin pisar contraseña/branding), rollback transaccional, y el gate RLS abortando el 2º tenant sin RLS y admitiéndolo con RLS. tsc en verde. **Falta para el alta REAL (gateada al 2º cliente):** activar RLS (ADR-018) y correr el script contra Neon con OK explícito — es el mismo trabajo (ADR-019 §2.d). *(origen: sesión de arquitectura — 2026-07-04 · implementado y verificado en sesión de feature — 2026-07-04)*
- [ ] `/sesion-feature importador CSV de clientes (ADR-019 §2.c, diferido)` — feature de migración de datos separada del alta; construir cuando exista un cliente concreto con lista real para importar (diseñar contra sus datos sucios de verdad). Nombre/teléfono/email, vista previa, tolerancia a datos sucios (ADR-009 §5). *(origen: sesión de arquitectura — 2026-07-04)*
- [x] **overbooking fail-safe (ADR-023 F2) — HECHO 2026-07-05** (sprint PMO "Tablero honesto + deuda técnica sin gates"). `isolationLevel: Serializable` + retry sobre `tenantTransaction`, expuesto como `bookingTransaction` (`src/lib/rls.ts`, reintenta ante `P2034`), aplicado a las **4** rutas de reserva (eran 3; se sumó la reprogramación pública `client-actions.ts`): `actions.ts` alta + reprogramación, `client-actions.ts`, `waitlist-actions.ts`. **ADR-004 enmendado** (§Enmienda 2026-07-05: Serializable como mecanismo Fase 1, `EXCLUDE USING GIST` como objetivo de plan pago) y **ADR-023 F2 marcado resuelto**. `tsc` + build en verde. *(origen: sesión de arquitectura — 2026-07-04 · resuelto en sprint PMO — 2026-07-05)*
- [x] **reportes con agregación en DB (ADR-023 F3) — HECHO 2026-07-05** (sprint PMO "Avanzamos todo"). `getReportData(rangeDays=90)` filtra por `{ tenantId, status: APPROVED, createdAt: {gte,lte} }` con **rango obligatorio** (default 90d, selector 30/90/180/365 en la UI vía `?dias=`) y `select` acotado; se acabó el escaneo de todo el histórico. Constantes movidas a `src/lib/report-config.ts` (actions.ts es `"use server"`, no puede exportar consts). Los breakdowns por día/prof/servicio siguen en app pero sobre el set acotado; `groupBy`/SQL-crudo puro para ellos quedó deferido (FK denorm o `date_trunc` tz-aware, no paga al volumen). Índice `[tenantId, status, createdAt]` a evaluar con RLS/F1. tsc + build en verde. *(origen: sesión de arquitectura — 2026-07-04 · resuelto en sprint PMO — 2026-07-05)*
- [x] **limpiezas de queries (ADR-023 F4/F5) — HECHO 2026-07-05** (sprint PMO "Tablero honesto"). (F4) el loop N+1 de recursos en `assertSlotAvailable` (`booking-core.ts`) se colapsó a una sola query `resourceId: { in: [...] }` + agregación por recurso en memoria (semántica idéntica: un turno con 2 recursos suma a ambos). (F5) `getClients` (`actions.ts`) pasó a `include: { _count: { select: { appointments: true } } }` y `ClientsList.tsx` consume `c._count.appointments` en vez de traer todos los turnos de todos los clientes para contar. `tsc` + build en verde. *(origen: sesión de arquitectura — 2026-07-04 · resuelto en sprint PMO — 2026-07-05)*
- [x] **Retención de `AuditLog` (ADR-023 F8) — mecanismo + política HECHOS 2026-07-05** (sprint PMO "Avanzamos todo"). Ventana ~12–18m (default 18, `AUDIT_RETENTION_MONTHS`); `src/lib/audit-retention.ts` (`purgeAuditLogs`, idempotente, platform-wide, usa `[tenantId, createdAt]` → sin migración) + `scripts/purge-audit-logs.ts` (`npm run purge-audit`, default dry-run, real solo con `--apply`). ADR-009 (retención) y ADR-007 (eje storage) enmendados. **Queda operativo (no código):** activar corrida periódica (cron/manual) y vigilar el % de storage de Neon; no se ejecutó la purga contra prod. *(origen: sesión de arquitectura — 2026-07-04 · resuelto en sprint PMO — 2026-07-05)*
- [ ] **OPERATIVO — activar la purga de `AuditLog` periódica** + monitorear % storage de Neon. El mecanismo ya está (`npm run purge-audit`); falta decidir dónde corre el cron (o correrla a mano cada X meses) y sumar el % de storage como métrica de gate a plan pago. No es sesión de código. *(origen: sprint PMO — 2026-07-05)*
- [ ] `/sesion-arquitectura adoptar un harness de tests` — **el repo tiene CERO tests automatizados** (no hay runner en `package.json` ni archivos `*.test.*`; lo de provisioning fueron checks ad-hoc con PGlite, no una suite). Gap real, y más notorio tras codificar el estándar "de élite" (`docs/ONBOARDING-EQUIPO.md`). Decidir con ADR: runner (candidato liviano: `node:test` built-in, cero dep + `tsx`; alternativa `vitest`), qué se prueba primero (lógica pura ya lista y sin DB: `auditRetentionCutoff`/`purgeAuditLogs` con cliente mock, `booking-core` reglas de choque, parseo de args de scripts), y cómo corre en el ciclo (script `test` + opcional en predeploy). Es decisión estructural → NO se bolterró mid-sprint a propósito. *(origen: sprint PMO — 2026-07-05)*
- [ ] `/sesion-arquitectura convención whereForTenant() (puente pre-RLS, ADR-023 F1)` — helper que garantice el predicado `tenantId` en actions nuevas mientras el aislamiento sea app-level. **Baja prioridad hasta cerca del 2º tenant:** hoy con 1 tenant + resolución fail-closed no aporta seguridad, y una vez activo RLS el motor lo impone igual → construirlo ahora sería especulativo (ADR-006). Se retoma junto con la activación de RLS. *(origen: sprint PMO — 2026-07-05)*
- [ ] **APLICAR RLS a producción — requiere OK explícito de Maxi (Gate 2).** El **diseño + los archivos ya están escritos y verificados offline** (sesión de seguridad multi-tenant, 2026-07-04): `prisma/rls/0001_enable_rls.sql` (data-driven: ENABLE RLS + policy `tenant_isolation` USING+WITH CHECK sobre `"tenantId" = current_setting('app.current_tenant_id', true)` en las 28 tablas con `tenantId`, fail-closed, cuid sin cast a uuid), `0002_app_role.sql` (rol `app_user` sin `BYPASSRLS`), `0003_force_rls_optional.sql` (hardening FORCE), `0001_rollback.sql`, `check-coverage.mjs` (red estática — **corrida en verde**, 28/28) y `verify-rls.mjs` (verificación funcional para la branch de Neon). La mitad app-level (`src/lib/tenant-context.ts` + `src/lib/rls.ts`, extensión `$allOperations` + contexto por request) está **escrita pero apagada** (no cableada al cliente vivo → cero cambio en prod). **Los SQL están FUERA de `prisma/migrations/` a propósito**: ningún `migrate deploy` los aplica solo. **Acción pendiente (tu OK):** ensayar `0001`+`0002` en branch de Neon con `verify-rls.mjs`, cablear la app a `rlsPrisma`+`tenantTransaction`, rotar `DATABASE_URL` a `app_user`, y recién ahí aplicar a prod junto con el alta del 2º tenant. Procedimiento completo en `prisma/rls/README.md`. *(origen: sesión de seguridad multi-tenant — 2026-07-04)*
- [ ] `/sesion-feature activar RLS de Postgres (ADR-018)` — **disparo: cuando se provisione el 2º tenant, no antes.** **Nota (ADR-023 F1):** esta activación es también el fix de performance de F1 — la policy `tenant_id = current_setting(...)` inyecta el predicado que hoy falta en las queries de agenda/dashboard/reportes y enciende los índices compuestos ya existentes. Sumar al checklist: `connection_limit` bajo (3-5) + validar prepared statements + `SET LOCAL` sobre el pooler en branch de Neon (ADR-023 F6). Como puente antes del gate, convención `whereForTenant()` para no olvidar el predicado en actions nuevas. Migración con `ENABLE ROW LEVEL SECURITY` + policies `USING (tenant_id = current_setting('app.current_tenant_id'))` en cada tabla de negocio; rol de app sin `BYPASSRLS`; extensión de Prisma (`$allOperations`) que envuelve cada operación en `$transaction` con `SET LOCAL app.current_tenant_id`; resolución de tenant por request (subdominio/sesión) en `tenant.ts` conservando el assert fail-closed como red. **Ensayo obligatorio en branch de Neon con tenant sintético antes de tocar producción.** *(origen: sesión de arquitectura — 2026-07-03)*
- [ ] **OBSERVABILIDAD — adoptar el contexto de request en las server actions.** La v2 (`src/lib/request-context.ts` + merge en `logger`) ya correlaciona por `requestId` los 4 endpoints máquina-a-máquina (API pública POST + `[code]`, webhook de MP, health) vía `withRequestId`. Falta el borde de las **server actions** (`src/lib/actions.ts` y afines): no hay un choke-point único en Next para envolverlas, así que se decidió a propósito NO cablearlas esta sesión (además `actions.ts` está co-editado por otros frentes). Camino: un helper `withAction(name, fn)` que abra `runInRequestContext` + `setRequestContext({ actor, tenantId })` con lo que ya resuelve `requireCapability`, y adoptarlo action por action. Bajo riesgo, aditivo. *(origen: sprint Plataforma — observabilidad v2 — 2026-07-05)*
- [ ] **OBSERVABILIDAD — readiness opcional en `/api/health` cuando haya presupuesto de DB.** Hoy el health es shallow a propósito (no pega a Neon, plan free). Con presupuesto: un `?deep=1` que corra `SELECT 1` con timeout corto y devuelva `db: "ok"|"down"` — sin volverlo el default para que un uptime monitor no queme compute. *(origen: sprint Plataforma — observabilidad v2 — 2026-07-05)*
- [x] **URLs GRATIS por tenant — override `FORCE_TENANT_SLUG` HECHO 2026-07-05 (en main).** Paso 0 de `getCurrentTenantId` (`src/lib/tenant.ts`): si `FORCE_TENANT_SLUG` está seteado, fija el tenant a ese slug (fail-closed si no existe), ignorando el host; si no, sigue el flujo subdominio→fallback. Habilita la **Opción A** del runbook (un sitio Netlify por tenant, mismo repo, gratis, sin dominio propio). `forcedTenantSlug`/`resolveForcedTenantId` con seam inyectable + tests; tsc+build+206 tests verde. Detalle: `docs/runbooks/alta-magra.md` §"URLs GRATIS por tenant". *(origen: sprint Plataforma — 2026-07-05)*
- [ ] **⚠️ `FORCE_TENANT_SLUG` NO reemplaza activar RLS.** Dos sitios Netlify pineados (CH y Magra) pegan a la **misma** Neon; el aislamiento por fila lo da RLS a nivel DB. Sin RLS on + `DATABASE_URL` en `app_user`, una query que olvide el predicado `tenantId` podría cruzar datos entre sitios. → La Opción A **sigue gateada** por la activación de RLS (ítems de arriba, Gate 2). *(origen: sprint Plataforma — 2026-07-05)*
- [ ] **FIX del root `/` blueprint-aware (deuda conocida, ESTADO-ACTUAL §6).** Hoy `/` sirve la landing de estética de CH (`src/app/(site)/page.tsx`), mientras la vidriera retail/carnicería vive en `/tienda`. Con `FORCE_TENANT_SLUG=magra`, `/tienda` muestra bien Magra pero `/` aún renderiza forma de spa. Hacer el root consciente del blueprint del tenant (retail/mostrador → vidriera de `/tienda`; servicios → landing `(site)`), o redirect `/`→`/tienda` para esos blueprints. Cambio de código verificable, no-prod. **Hacer antes de anunciar la URL pública de Magra.** *(origen: sprint Plataforma — 2026-07-05)*

- [x] **ENSAYO RLS offline (gate + aislamiento) — PASADO 2026-07-05.** `prisma/rls/verify-provision-gate.mts` (PGlite, cost 0, no toca Neon/prod) reproduce el ensayo del branch: schema real + `provisionTenant` real + `0001_enable_rls.sql` real. Verde en las 9 aserciones: alta #1, **gate SIN RLS bloquea el 2º tenant**, aplica RLS + `app_user`, **gate CON RLS abre → alta #2 pasa**, aislamiento `app_user` (cada tenant ve solo lo suyo), fail-closed sin contexto. **🐞 Bug encontrado y CORREGIDO:** `RLS_SENTINEL_TABLES` incluía `Tenant`, pero `0001` la excluye (raíz sin `tenantId`) → `isRlsActive` daba false para siempre; el gate del 2º tenant **nunca habría abierto en prod aun con RLS aplicado**. Fix: sentinel = `["Appointment","Client"]` (`scripts/provision-tenant.ts`). tsc+build+206 tests+ensayo verde. *(origen: ensayo RLS — sprint Plataforma — 2026-07-05)*
- [ ] **ENSAYO RLS en branch de Neon REAL (pendiente — necesita acción del dueño).** El ensayo offline (arriba) cubre la lógica; falta el ensayo contra un **branch de Neon** (copia de prod) que valida lo que PGlite no: pooler de Neon, prepared statements, `connection_limit` bajo, y el schema/datos reales. Bloqueado por infra: no hay `neonctl`/`NEON_API_KEY`/`psql` en el entorno → **el dueño crea el branch en la consola de Neon y pasa la URL**, o provee un `NEON_API_KEY`. Comandos exactos en `docs/runbooks/alta-magra.md` Paso 1 (`psql -f 0001`/`0002` + `RLS_VERIFY_DATABASE_URL=… node prisma/rls/verify-rls.mjs`). *(origen: ensayo RLS — sprint Plataforma — 2026-07-05)*

- [x] **HALLAZGO CRÍTICO + fixes de repo (2026-07-05): prod RLS con drift + `app_user` con BYPASSRLS.** Auditoría solo-lectura de prod (PASO 0 del go-live) encontró que RLS ya está aplicado en **24/33** tablas; **9 de-tenant sin proteger** (`Order/OrderItem/Invoice/OutboxEvent/CashMovement/CashSession/StockMovement/StockPurchase/StockPurchaseItem`) y **`app_user` con `BYPASSRLS=true`** (rotar a él daría cero aislamiento). **Se paró el go-live antes de tocar prod.** Fixes de repo (en main, sin tocar prod): (a) `0002_app_role.sql` fuerza `ALTER ROLE app_user … NOBYPASSRLS` aunque el rol ya exista; (b) `prisma/rls/check-rls-live.mjs` — auditoría del drift en vivo (solo lectura, branch/prod); (c) `verify-provision-gate.mts` extendido: cobertura 33/33 + regresión del footgun. Verificado offline (PGlite): re-correr `0001` cierra el drift (33/33), `0002` deja `app_user` sin bypass. *(origen: go-live Magra — sprint Plataforma — 2026-07-05)*
- [ ] **BLOQUEADO — ensayo en branch de Neon + go-live de Magra.** Necesita del dueño: **(a)** un `NEON_API_KEY` (o que cree el branch y pase la URL) para el ensayo seguro; **(b)** que el ensayo pase con la secuencia corregida (audit → `0001`+`0002` → audit 33/33 + `app_user` sin bypass → `verify-rls`); **(c)** OK final para prod. Recién ahí: aplicar a prod, rotar secrets (los carga el dueño), alta de Magra, deploy `magra-erp` con `FORCE_TENANT_SLUG=magra`. Guion completo en `docs/runbooks/alta-magra.md`. *(origen: go-live Magra — 2026-07-05)*

## Hechos (podados por `/sesion-consolidacion` 2026-07-04)

_Barridos en la consolidación del 2026-07-04 tras verificar contra `git log` de `main` y el BACKLOG que están hechos y deployados: Liquidación de comisiones (+ migración `20260704130000`), Módulo Localización (+ migración `20260704120000`), Cliente reprograma su turno, RBAC Fase 1 y Fase 2. Rastro completo en el historial de git y en `BACKLOG.md`._

_Mergeado en esta misma consolidación (2026-07-04): **Lista de espera** (`feature/lista-de-espera`, feature P0 de `docs/ANALISIS-BRECHAS.md`) — merge a `main`, migración `20260704140000_add_waitlist` aplicada a Neon, build en verde; y **upgrade UX/UI base** (`feature/ux-ui-upgrade`) — capa de design tokens semánticos + primitivos `src/components/ui/`, aditiva y aún sin consumir en pantallas. Queda fuera de main a propósito: el **script de provisioning** (`feature/performance`, ADR-019) — ver Abiertos._
