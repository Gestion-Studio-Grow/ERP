# La página armada (~3 MB) se REGENERA con el helper a partir de los .dc.html + canvas.json
# + las fotos de public/tenants/shinevelas. No la versionamos: lo que vale son las fuentes.
shop-de-shine.html
