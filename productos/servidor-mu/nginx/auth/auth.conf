# Generado por ./scripts/levantar.sh — NO lo edites a mano, se pisa en cada levantada.
#
# Con el panel en loopback (127.0.0.1) este archivo queda vacío: el único que puede
# llegar al panel es quien ya está sentado en esta máquina.
# Si exponés el panel a la red (PANEL_BIND_ADDR distinto de 127.x), levantar.sh escribe
# acá un auth_basic que pide usuario y contraseña ANTES de que el request llegue al panel.
