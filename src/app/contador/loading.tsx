// Skeleton de carga del panel del contador (gate UX/UI, fix 4) — la misma
// anatomía honesta que el del backoffice (PageHeader + 4 KPIs + tabla): el
// panel del contador comparte piel (skin Fable) y estructura con /admin.
export { default } from "@/app/admin/(dashboard)/loading";
